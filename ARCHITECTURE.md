# 🏗️ machi00_shad Architecture

This document describes the technical architecture of the machi00_shad platform.

## 📊 Overview

```
┌────────────────────────────────────────────────────────────────┐
│                            USER                                │
└────────────────────────────┬───────────────────────────────────┘
                             │
                             ▼
┌────────────────────────────────────────────────────────────────┐
│                      FRONTEND (Vue.js 3)                       │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │  Components  │  │    Pinia     │  │   Router     │          │
│  │   (UI/UX)    │◄─┤   (State)    │◄─┤  (Routes)    │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│         │                  │                                   │
│         └──────────────────┴─────────────────┐                 │
│                                               ▼                │
│                                    ┌──────────────────┐        │
│                                    │  API Service     │        │
│                                    │  (HTTP Client)   │        │
│                                    └────────┬─────────┘        │
└─────────────────────────────────────────────┼──────────────────┘
                                              │
                                   HTTP/WebSocket
                                              │
                                              ▼
┌────────────────────────────────────────────────────────────────┐
│                    BACKEND (Node.js/Express)                   │
│                                                                │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                        ROUTES                            │  │
│  │  /api/auth  /api/orders  /api/market  /api/strategies    │  │
│  └────────────────────────┬─────────────────────────────────┘  │
│                           │                                    │
│                           ▼                                    │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                     CONTROLLERS                          │  │
│  │  authCtrl  orderCtrl  marketCtrl  strategyCtrl           │  │
│  └────────────────────────┬─────────────────────────────────┘  │
│                           │                                    │
│                           ▼                                    │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                      SERVICES                            │  │
│  │  Trading  MarketData  Strategy  Notification  Cron       │  │
│  └────────────────────────┬─────────────────────────────────┘  │
│                           │                                    │
│                           ▼                                    │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    REPOSITORIES                          │  │
│  │  OrderRepo  UserRepo  MarketRepo  StrategyRepo           │  │
│  └────────────────────────┬─────────────────────────────────┘  │
└───────────────────────────┼────────────────────────────────────┘
                            │
                            ▼
        ┌───────────────────────────────────────┐
        │         DATA LAYER                    │
        │                                       │
        │  ┌─────────────┐    ┌──────────────┐  │
        │  │   MongoDB   │    │  External    │  │
        │  │  (Database) │    │  APIs (CCXT) │  │
        │  └─────────────┘    └──────────────┘  │
        └───────────────────────────────────────┘
```

## 🎨 Frontend Architecture

### Composition API Pattern

```
src/
├── components/          # Reusable Vue components
│   ├── Dashboard/
│   │   ├── DashboardView.vue
│   │   ├── WidgetCard.vue
│   │   └── ChartWidget.vue
│   ├── Orders/
│   │   ├── OrderList.vue
│   │   ├── OrderCard.vue
│   │   └── OrderForm.vue
│   └── Common/
│       ├── Button.vue
│       ├── Modal.vue
│       └── Loading.vue
│
├── composables/         # Reusable logic (Composition API)
│   ├── useAuth.ts       # Authentication
│   ├── useOrders.ts     # Order management
│   ├── useMarket.ts     # Market data
│   └── useWebSocket.ts  # Real-time connection
│
├── store/               # Pinia stores (global state)
│   ├── auth.ts
│   ├── orders.ts
│   └── market.ts
│
├── services/            # API services
│   └── api.ts
│
└── types/               # TypeScript types
    ├── Order.ts
    └── Market.ts
```

### Frontend Data Flow

```
User Action
    │
    ▼
Component (Vue)
    │
    ├─► Composable (useOrders)
    │       │
    │       ├─► API Service
    │       │       │
    │       │       └─► Backend API
    │       │
    │       └─► Pinia Store (update state)
    │
    └─► Template (reactive update)
```

## ⚙️ Backend Architecture

### Layered Architecture (MVC)

```
Request Flow:
─────────────

HTTP Request
    │
    ▼
┌─────────────────┐
│     ROUTES      │  ← Endpoint definitions
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  MIDDLEWARES    │  ← Auth, Validation, Error Handling
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  CONTROLLERS    │  ← Control logic (orchestration)
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│    SERVICES     │  ← Business logic
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  REPOSITORIES   │  ← Data access
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│    DATABASE     │  ← MongoDB
└─────────────────┘
```

### Concrete Example: Create an Order

```typescript
// 1. ROUTE (routes/orderRoutes.ts)
router.post('/api/orders', authMiddleware, orderController.create)

// 2. MIDDLEWARE (middlewares/authMiddleware.ts)
// Verifies JWT and adds req.user

// 3. CONTROLLER (ctrl/orderController.ts)
async create(req, res) {
  const orderData = req.body
  const userId = req.user.id

  const order = await orderService.createOrder(userId, orderData)

  res.json({ success: true, order })
}

// 4. SERVICE (services/tradingService.ts)
async createOrder(userId, orderData) {
  // Business validation
  // Call exchange via CCXT
  const exchangeOrder = await ccxt.createOrder(...)

  // Save to DB
  const order = await orderRepository.save({
    userId,
    ...orderData,
    exchangeOrderId: exchangeOrder.id
  })

  return order
}

// 5. REPOSITORY (repo/orderRepository.ts)
async save(orderData) {
  const collection = db.collection('orders')
  const result = await collection.insertOne(orderData)
  return result
}
```

## 🔄 Real-time Data Flow

### WebSocket Architecture

```
┌──────────────┐                    ┌──────────────┐
│   Frontend   │                    │   Backend    │
│              │                    │              │
│  WebSocket   │◄───────────────────┤  WebSocket   │
│   Client     │     Connection     │   Server     │
│              │                    │              │
└──────┬───────┘                    └──────┬───────┘
       │                                   │
       │  Subscribe to BTC/USDT            │
       ├──────────────────────────────────►│
       │                                   │
       │                                   │ Fetch from
       │                                   │ Exchange
       │                                   ├──────────┐
       │                                   │          │
       │                                   │◄─────────┘
       │                                   │
       │  Price Update: 45000              │
       │◄──────────────────────────────────┤
       │                                   │
       │  Update UI                        │
       ├─────────┐                         │
       │         │                         │
       │◄────────┘                         │
       │                                   │
```

## 🗄️ Data Model

### MongoDB Collections

#### Users

```typescript
{
  _id: ObjectId,
  email: string,
  password: string (hashed),
  name: string,
  apiKeys: {
    binance: {
      key: string (encrypted),
      secret: string (encrypted)
    }
  },
  createdAt: Date,
  updatedAt: Date
}
```

#### Orders

```typescript
{
  _id: ObjectId,
  userId: ObjectId,
  symbol: string,        // "BTC/USDT"
  type: string,          // "market" | "limit" | "stop-loss"
  side: string,          // "buy" | "sell"
  amount: number,
  price?: number,
  status: string,        // "open" | "closed" | "cancelled"
  exchangeOrderId: string,
  exchange: string,      // "binance"
  createdAt: Date,
  updatedAt: Date
}
```

#### Strategies

```typescript
{
  _id: ObjectId,
  userId: ObjectId,
  name: string,
  type: string,          // "dca" | "grid" | "custom"
  config: {
    symbol: string,
    interval: string,
    conditions: object
  },
  status: string,        // "active" | "paused" | "stopped"
  performance: {
    totalProfit: number,
    winRate: number,
    trades: number
  },
  createdAt: Date,
  updatedAt: Date
}
```

## 🔐 Security

### Authentication Flow

```
1. Login Request
   ┌──────────┐         ┌──────────┐
   │ Frontend │────────►│ Backend  │
   └──────────┘         └────┬─────┘
                             │
                             ▼
                        Verify credentials
                             │
                             ▼
                        Generate JWT
                             │
                             ▼
   ┌──────────┐         ┌──────────┐
   │ Frontend │◄────────┤ Backend  │
   └────┬─────┘         └──────────┘
        │
        ▼
   Store JWT in
   localStorage

2. Authenticated Request
   ┌──────────┐         ┌──────────┐
   │ Frontend │────────►│ Backend  │
   └──────────┘         └────┬─────┘
   Header:                   │
   Authorization:            ▼
   Bearer <JWT>         Verify JWT
                             │
                             ▼
                        Extract user
                             │
                             ▼
                        Process request
                             │
                             ▼
   ┌──────────┐         ┌──────────┐
   │ Frontend │◄────────┤ Backend  │
   └──────────┘         └──────────┘
```

### API Key Encryption

```typescript
// Encryption
const cipher = crypto.createCipher("aes-256-cbc", ENCRYPTION_KEY);
let encrypted = cipher.update(apiKey, "utf8", "hex");
encrypted += cipher.final("hex");

// Decryption
const decipher = crypto.createDecipher("aes-256-cbc", ENCRYPTION_KEY);
let decrypted = decipher.update(encryptedKey, "hex", "utf8");
decrypted += decipher.final("utf8");
```

## 📊 Exchange Integration

### CCXT Abstraction Layer

```
┌─────────────────────────────────────────────────┐
│              machi00_shad Backend               │
│                                                 │
│  ┌───────────────────────────────────────────┐  │
│  │         Trading Service                   │  │
│  └─────────────────┬─────────────────────────┘  │
│                    │                            │
│                    ▼                            │
│  ┌───────────────────────────────────────────┐  │
│  │              CCXT Library                 │  │
│  │  (Unified API for all exchanges)          │  │
│  └───┬────────────┬────────────┬─────────────┘  │
└──────┼────────────┼────────────┼────────────────┘
       │            │            │
       ▼            ▼            ▼
┌──────────┐  ┌──────────┐  ┌──────────┐
│ Binance  │  │  Kraken  │  │ Coinbase │
│   API    │  │   API    │  │   API    │
└──────────┘  └──────────┘  └──────────┘
```

### CCXT Usage Example

```typescript
import ccxt from "ccxt";

// Initialization
const exchange = new ccxt.binance({
  apiKey: decryptedApiKey,
  secret: decryptedSecret,
});

// Fetch price
const ticker = await exchange.fetchTicker("BTC/USDT");

// Create order
const order = await exchange.createOrder(
  "BTC/USDT",
  "limit",
  "buy",
  0.01,
  45000
);
```

## 🔄 Automated Tasks (Cron Jobs)

```
┌─────────────────────────────────────────────────┐
│           Cron Service (node-cron)              │
├─────────────────────────────────────────────────┤
│                                                 │
│  Every 30s  ──► Update Market Prices            │
│                                                 │
│  Every 1m   ──► Check Alarms                    │
│                                                 │
│  Every 5m   ──► Update CoinMarketCap Data       │
│                                                 │
│  Daily 3am  ──► Clean Old Logs                  │
│                                                 │
│  Daily 4am  ──► Backup Database                 │
│                                                 │
└─────────────────────────────────────────────────┘
```

## 🚀 Deployment

### Production Architecture

```
┌─────────────────────────────────────────────────┐
│              CDN (Cloudflare)                   │
│         Static Assets (Frontend)                │
└────────────────┬────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────┐
│         Load Balancer (Nginx)                   │
└────────┬────────────────────────┬───────────────┘
         │                        │
         ▼                        ▼
┌─────────────────┐      ┌─────────────────┐
│  Backend API    │      │  Backend API    │
│  Instance 1     │      │  Instance 2     │
└────────┬────────┘      └────────┬────────┘
         │                        │
         └────────────┬───────────┘
                      │
                      ▼
         ┌────────────────────────┐
         │   MongoDB Atlas        │
         │   (Replica Set)        │
         └────────────────────────┘
```

## 📈 Scalability

### Scaling Strategies

1. **Horizontal Scaling** - Add more backend instances
2. **Caching** - Redis for frequently accessed data
3. **Database Sharding** - Partition MongoDB by userId
4. **CDN** - Serve static assets via CDN
5. **Microservices** - Separate trading, market data, notifications

## 🔍 Monitoring

### Key Metrics

```
┌─────────────────────────────────────────────────┐
│              Monitoring Stack                   │
├─────────────────────────────────────────────────┤
│                                                 │
│  Application Metrics (PM2/New Relic)            │
│  ├─ Response Time                               │
│  ├─ Request Rate                                │
│  ├─ Error Rate                                  │
│  └─ CPU/Memory Usage                            │
│                                                 │
│  Business Metrics (Custom)                      │
│  ├─ Active Orders                               │
│  ├─ Trading Volume                              │
│  ├─ Active Strategies                           │
│  └─ User Activity                               │
│                                                 │
│  Infrastructure (AWS CloudWatch)                │
│  ├─ Server Health                               │
│  ├─ Database Performance                        │
│  └─ Network Traffic                             │
│                                                 │
└─────────────────────────────────────────────────┘
```

## 📚 Resources

- [Vue.js Architecture Guide](https://vuejs.org/guide/scaling-up/state-management.html)
- [Express.js Best Practices](https://expressjs.com/en/advanced/best-practice-performance.html)
- [MongoDB Schema Design](https://www.mongodb.com/docs/manual/core/data-modeling-introduction/)
- [CCXT Documentation](https://docs.ccxt.com/)

---

[⬆ Back to main README](./README.md)
